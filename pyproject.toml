[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qa-automation-journey"
version = "0.1.0"
description = "QA Automation Journey"
dependencies = ["pytest", "playwright", "requests"]

[tool.pytest.ini_options]
testpaths = ["tests"]